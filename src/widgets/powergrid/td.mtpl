<% var v = this.getValue(data),
       formatted = this.formatValue(v, data),
       title = this.getTitle(formatted, v, data),
       value = (formatted == null)? '' : formatted.toString();
%>
<td class="<%=this.cssClasses(data)%>" title="<%=title%>">
  <%= value %>
</td>
